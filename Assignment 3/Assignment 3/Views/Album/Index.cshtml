@model IEnumerable<Assignment_3.Models.AlbumBaseViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Albums</h2>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
    </tr>
}

</table>
